name: Tests
on:
  workflow_dispatch:
jobs:
  unitTests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: recursive
      - name: Build unit tests
        run: |
          cd test/unit
          make
      - name: Run unit tests
        run: |
          cd test/unit
          make run
