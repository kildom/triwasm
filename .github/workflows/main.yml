name: Tests
on:
  workflow_dispatch:
jobs:
  triasm-node:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
      - name: npm update
        run: npm update
      - name: Run tests
        run: |
          cd test/asm
          npx ts-node vectors.ts
      - name: Build
        run: npm run build
  triasm-quickjs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
      - uses: actions/checkout@v3
        with:
          path: quickjs
          repository: bellard/quickjs
          submodules: recursive
      - name: npm update
        run: npm update
      - name: Compile QuickJS
        run: |
          cd quickjs
          make -j`nproc`
      - name: Run tests
        run: |
          npx tsc -p test/asm/vectors.tsconfig.json
          cd test/asm
          #time ../../quickjs/qjs --std vectors.js
          echo "import * as std from 'std';" > tmp.txt
          echo "import * as os from 'os';" >> tmp.txt
          cat tmp.txt ../output/asm/vectors.js > tmp.js
          ../../quickjs/qjsc \
            -flto -fbignum -fno-string-normalize -fno-module-loader -m \
            -o vectors tmp.js
          strip vectors
          time ./vectors
